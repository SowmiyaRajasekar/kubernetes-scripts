kind: Pod
apiVersion: v1
metadata:
  name: labelpod1
  labels:
    app: myapp
    env: prod
spec:
  containers:
    - name: c1
      image: nginx
      command: ["/bin/bash", "-c","while true; do echo Hello-sai; sleep 5; done"]


